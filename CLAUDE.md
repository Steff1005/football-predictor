@AGENTS.md

# Інструкції для Claude Code

## Деплой
НІКОЛИ не використовувати `npx vercel` або `vercel --prod`.
Для деплою тільки: `git add . && git commit -m "..." && git push`
Vercel автоматично задеплоїть зміни з GitHub.
